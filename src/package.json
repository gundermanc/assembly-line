{
  "name": "assembly-line",
  "version": "1.0.0",
  "description": "A transpilation language for your business logic.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "npm install && tsc -p tsconfig.json",
    "lint": "eslint --ext .ts **/*.ts",
    "run": "npm run build && node ../dist/index.js"
  },
  "keywords": [],
  "author": "Christian Gunderman",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.5.7",
    "@typescript-eslint/eslint-plugin": "^6.5.0",
    "eslint": "^8.48.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
  }
}
